export enum ButtonVariant {
    LIGHT = 'light',
    DARK = 'dark'
}

export enum AddProductType {
    ITEM = 'item',
    PRODUCT = 'product'
}

export enum SubmitType {
    ADD = 'add',
    EDIT = 'edit'
}